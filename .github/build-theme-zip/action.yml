name: 'Auto Generate build-theme-zip.temp.sh file'
description: 'Generating temporary bash file to use of that to create blockera-one.zip file.'

runs:
    using: 'composite'
    steps:
        - name: Regenerating Bash Script
          run: php ./bin/generate-build-theme-zip-sh.php > ./bin/build-theme-zip.temp.sh
          shell: bash

        - name: CHMOD
          run: chmod +x ./bin/build-theme-zip.temp.sh
          shell: bash

        - name: Execute Created Temporary Bash Script
          run: ./bin/build-theme-zip.temp.sh
          env:
              NO_CHECKS: 'true'
              NO_INSTALL_NPM: 'true'
              NO_INSTALL_COMPOSER: 'true'
          shell: bash

        - name: Delete Permanently Temporary File
          run: rm -rf ./bin/build-theme-zip.temp.sh
          shell: bash
