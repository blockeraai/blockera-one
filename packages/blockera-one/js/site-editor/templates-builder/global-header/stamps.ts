/**
 * Global header template-part stamp dictionary. Ids must stay unique across
 * template types (`section/header` already lives in shared stamps).
 */

import type { StampDictionaryEntry } from '../shared/stamp';

export const GLOBAL_HEADER_STAMPS: readonly StampDictionaryEntry[] = [
	'layout/header-body',
];
