/**
 * Global sidebar template-part stamp dictionary. Ids must stay unique across
 * template types (`section/sidebar` already lives on archive).
 */

import type { StampDictionaryEntry } from '../shared/stamp';

export const GLOBAL_SIDEBAR_STAMPS: readonly StampDictionaryEntry[] = [
	'layout/sidebar-body',
];
