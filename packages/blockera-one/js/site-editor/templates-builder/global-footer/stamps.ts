/**
 * Global footer template-part stamp dictionary. Ids must stay unique across
 * template types (`section/footer` already lives in shared stamps).
 */

import type { StampDictionaryEntry } from '../shared/stamp';

export const GLOBAL_FOOTER_STAMPS: readonly StampDictionaryEntry[] = [
	'layout/footer-body',
];
